#include <bits/stdc++.h>

using namespace std;

int main() {

    int test, n, r;
    cin >> test;
    while(test--) {
        cin >> n;
        r = sqrt(n);
        cout << r << endl;
    }

    return (0);
}
