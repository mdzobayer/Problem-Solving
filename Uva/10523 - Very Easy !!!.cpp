#include <bits/stdc++.h>
#define ll long long int

using namespace std;

int main () {

    ll n, a, sum;

    while(scanf("%lld %lld", &n, &a) == 2) {
        if (a == 0) {
            puts("0");
            continue;
        }

        sum = 0;

        for (i = 1; i <= n; ++i) {
            sum += (i * )
        }

    }

    return (0);
}