#include <cstdio>
#include <queue>

int main() {
    int p, c, i;

    while(scanf("%d %d", &p, &c) == 2) {
        if (p == 0 && c == 0)
            break;
        for (i = 0; i < c; ++i) {

        }

    }
    return (0);
}
