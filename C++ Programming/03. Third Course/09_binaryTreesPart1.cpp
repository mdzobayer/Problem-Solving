#include <iostream>

using namespace std;

class Node {
public:
    string data;
    Node *left;
    Node *right;
    Node(string d) {
        data = d;
        left = NULL;
        right = NULL;
    }
};

int main() {
    /*
    A binary tree is a data structure made of nodes
    with a root node. An under a root node will be a
    series of child nodes. For binary tree that means
    a left child and a right child nodes only other is
    such a things a tree data structures where you can
    have it's many child nodes under a parent as necessary
    but for a binary tree you can only have two child nodes.
    Left and right node.
    */

    return (0);
};
