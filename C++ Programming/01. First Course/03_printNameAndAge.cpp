#include <iostream>

using namespace std;
int main()
{
    char name[10];
    int age;
    cout<<"Enter your name: ";
    cin>>name;
    cout<<"Enter your age: ";
    cin>>age;

    cout<<"Name = "<<name<<" and the age is "<<age;

    return (0);
}
