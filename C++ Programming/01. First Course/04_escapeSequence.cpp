#include <iostream>

int main(){
    using namespace std;
    int age;
    char name[32];
    cout<<"Enter the name: ";
    cin>>name;
    cout<<"Enter your age: ";
    cin>>age;
    cout<<"The name of the person is "<<name<<" and the age is "<<age<<"."<<endl;

    return (0);
}
