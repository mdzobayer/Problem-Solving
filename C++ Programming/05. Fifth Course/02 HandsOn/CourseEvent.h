#pragma once
// Include any files necessary here.

int main() {
    //Declare a Course variable below.

    //Declare a pointer to a Person variable below.

    //Create a person at the end of the pointer (hint new Person) initializing
    //it with universal initialization (the actual data used is unimportant).

    //Add the person you have just crated to the Course.

}
