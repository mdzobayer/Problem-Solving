#include <iostream>
#include <map>

using namespace std;

int main() {
    map<string, int> cycle;
    ios::sync_with_stdio(false);
    string s;
    int test;

    cin >> test;

    while(test--) {
        cycle.clear();
        cin >> s;

    }

    return (0);
}
