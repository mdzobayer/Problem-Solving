#include <bits/stdc++.h>

using namespace std;


int main() {

    int k, q, r, b, kn, p;
    cin >> k >> q >> r >> b >> kn >> p;

    printf("%d %d %d %d %d %d\n", 1 - k, 1 - q, 2 - r, 2 - b, 2 - kn, 8 - p);

    return (0);
}
