#include <bits/stdc++.h>

using namespace std;

int main() {
    int i, hava, n, sz;
    string s;
    cin >> s;
    have = n = 0;
    sz = s.size();
    for ( i = 0; i < sz; ++i) {

    }

    return (0);
}
