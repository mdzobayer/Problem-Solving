#include <bits/stdc++.h>

using namespace std;

int main() {

    int test, d;
    scanf("%d", &test);

    while(test--) {
        scanf("%d", &d);

        for (a = - d + 1; a < d; ++a) {

        }
    }

    return (0);
}
