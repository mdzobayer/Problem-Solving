#include <bits/stdc++.h>

using namespace std;

int arr[2000+5];

int main() {

    int i, n, k;
    cin >> n >> k;
    for (i = 0; i < n; ++i) {
        cin >> arr[i];
    }

    


    return (0);
}